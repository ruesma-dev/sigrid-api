<!-- specs/F-006-alta-parte-reclamacion/tasks.md -->
# F-006 · Tareas

Todas las lecturas contra el ERP van por `sql/read` de la API desplegada (`SIGRID_API_BASE_URL` + `SIGRID_API_FUNCTION_KEY`). Cualquier `commit:true` exige autorización expresa del humano para esa llamada concreta. Ningún test toca red ni BBDD.

- [x] T1: **HECHA el 2026-09-24 (solo lectura)** — modelo de filas medido en `progress/explore_F-006_modelo_parte.md`: tablas (§1), columnas y orígenes (§2), numeración (§3), estado inicial (§4), referencia externa (§5), resolución por códigos (§6). El implementer **copia las constantes de `design.md` §Filas**, no las vuelve a medir.  |  Verificación: informe existente; `design.md` §Filas cita cada constante
- [x] T2: **HECHA el 2026-09-24** — respuestas del humano a Q1-Q4 registradas en `progress/current.md` y aplicadas a la spec (`requirements.md` §Decisiones): prefijo `PVI-`, `forma_comunicacion` 1, fila de `log` sí, obra `0626` / UPV `0626.03PORTAL 1.1.A`.  |  Verificación: `progress/current.md` las recoge con fecha
- [x] T3: Escribir `tests/test_f006_settings.py` (R4, R5) en ROJO: `Settings` con el entorno aislado arranca con los cuatro defectos; `SIGRID_RECLAMACION_PREFIJOS_REFERENCIA` acepta JSON y CSV; `ALLOWED_WRITE_DATABASES` no cambia.  |  Verificación: `python -m pytest tests/test_f006_settings.py -q` falla por atributo inexistente (traza en `impl_F-006.md`)
- [x] T4: Añadir los cuatro campos a `config/settings.py` y las claves a `local.settings.sample.json`.  |  Verificación: `python -m pytest tests/test_f006_settings.py -q` en verde
- [x] T5: Escribir `tests/test_f006_models.py` (R1, R2) en ROJO: obligatorios, longitudes (24/80/128/48), `tipo` por defecto `"0002"`, `forma_comunicacion` por defecto 1 y solo 0/1, `intervinientes` ≤10, `partes` ≥1, `extra="forbid"` rechaza `ide`/`cod`/`emp`/`est`, `commit` por defecto `false`, textos vacíos tras `strip` rechazados, y `ParteReclamacionError` solo admite los códigos cerrados.  |  Verificación: `python -m pytest tests/test_f006_models.py -q` falla por módulo inexistente
- [x] T6: Crear `domain/models/parte_reclamacion_models.py`.  |  Verificación: `python -m pytest tests/test_f006_models.py -q` en verde
- [x] T7: Escribir `tests/test_f006_statements.py` (R11, R12, R16, R18) en ROJO: SQL exacto carácter a carácter de L1-L10 y E1-E14; listas completas de columnas de los cinco `INSERT` (19/20/5/11/14) con las constantes de `design.md` §Filas; `rcp.ide`, `rcpint.rcpide` y `conext.conide` son el `con.ide`; `log.cod`/`res`/`est`/`fec`/`hor` iguales a los de `con`/`rcp`; `tiemod` OLE UTC para instantes conocidos (verano e invierno); `siguiente_cod` (`None`→`0001`, `…/0771`→`…/0772`, `…/9999`→`numeracion_agotada`); patrón `LIKE` con `[0-9]`×4; `UPDLOCK, HOLDLOCK` en E2-E7; control negativo de `UPDATE`/`DELETE`/`MERGE`/DDL; `DatabaseReferenceGuard` no rechaza ninguna con `allowed=["ruesma"]`.  |  Verificación: `python -m pytest tests/test_f006_statements.py -q` falla por módulo inexistente
- [x] T8: Crear `application/use_cases/parte_reclamacion_statements.py` (importa `hora_local_de_madrid` de `concepto_grafico_statements.py`, sin editarlo).  |  Verificación: `python -m pytest tests/test_f006_statements.py -q` en verde
- [x] T9: Escribir `tests/test_f006_use_case.py` (R3, R6-R10, R13-R15, R17, R19, R20) en ROJO con repositorio, cursor y reloj falsos: errores de lote (llaves, base, base con lista vacía, tope, obra, usu, serie, estado); cada código de rechazo de R7-R8 sin afectar al parte siguiente; dry-run sin `run_in_write_transaction` con `cod` provisionales consecutivos; commit con una transacción por parte y applocks en el orden de `design.md`; `work` ejecuta E1-E14 en orden y rellena `ide`/`cod`/`pos` en cada intento; `IntegrityError` → `colision_de_clave` y el lote sigue; relectura ≠ esperado → `filas_afectadas_inesperadas`; idempotente sin escribir y `referencia_en_conflicto`; referencia repetida en el lote; presupuesto agotado → `no_procesado`; ninguna sentencia toca `tar`; la traza no contiene las descripciones.  |  Verificación: `python -m pytest tests/test_f006_use_case.py -q` falla
- [x] T10: Crear `application/use_cases/create_partes_reclamacion_use_case.py`.  |  Verificación: `python -m pytest tests/test_f006_use_case.py -q` en verde
- [x] T11: Escribir `tests/test_f006_route.py` (R2, R3) en ROJO con `func.HttpRequest` falso y registrar la ruta `sigrid/partes-reclamacion` en `function_app.py` sin editar las existentes: 200 con rechazados, 400 con `details.codigo`, 400 de Pydantic, 500 en lo inesperado.  |  Verificación: `python -m pytest -k f006 -q` en verde y `git diff function_app.py` solo añade
- [x] T12: Suite completa y cobertura (R5, R21): los tests de F-003, F-004 y F-005 siguen en verde.  |  Verificación: `python -m pytest -q` en verde y `bash harness/init.sh` con cobertura en `[OK]`
- [x] T13: Campaña de mutación completa (rigor `critico`, sin tope): cero supervivientes o equivalentes con demostración ejecutable aceptados por el humano.  |  Verificación: `python -m harness.mutacion --feature F-006` → `progress/mutacion_F-006.md`
- [x] T14: Documentación (R23): `docs/ARCHITECTURE.md` (ruta, modelo, caso de uso), mapa de rutas de `CLAUDE.md`, y `azure-apps/sigrid_api.md` §4 (cuatro App Settings), §7.1/§7.6 (fila nueva: qué escribe y qué no recalcula), §7.5 (código de serie dentro de la transacción), §8.9 nueva (petición, respuesta, códigos, idempotencia por `RCPCLI`), §9.2 (`tip` 707/708) y §10 (`postventa-incidencias` F-040). Commit en `azure-apps` aparte.  |  Verificación: el reviewer coteja lo escrito con el código y con `explore_F-006_modelo_parte.md`
- [ ] T15: **MANUAL (humano)** — desplegar (`func azure functionapp publish func-sigridapi-dev-huyke --python`) y fijar por fichero JSON (ASCII sin BOM) `SIGRID_RECLAMACION_WRITE_ENABLED=false`, `SIGRID_RECLAMACION_MAX_PARTES=50`, `SIGRID_RECLAMACION_PREFIJOS_REFERENCIA=["PVI-"]`, `SIGRID_RECLAMACION_PRESUPUESTO_SEGUNDOS=150` con `az functionapp config appsettings set -g rg-sigrid-dev-data-api -n func-sigridapi-dev-huyke --settings "@f006_appsettings.json"`.  |  Verificación: `az functionapp config appsettings list -g rg-sigrid-dev-data-api -n func-sigridapi-dev-huyke --query "[?starts_with(name,'SIGRID_RECLAMACION') || name=='ALLOWED_WRITE_DATABASES' || name=='SIGRID_DOMAIN_WRITE_ENABLED']"` muestra los cuatro valores y `ALLOWED_WRITE_DATABASES` en `["ruesma"]`
- [ ] T16: **MANUAL (humano)** — dry-run (100 % lectura): `POST $BASE/api/sigrid/partes-reclamacion` sin `commit` con este lote literal de tres partes: `{"database":"ruesma","obra":"0626","usu":"prueba","partes":[{"referencia_externa":"PVI-PRUEBA-0001","unidad_postventa":"0626.03PORTAL 1.1.A","descripcion":"PRUEBA API - ANULAR","oficio":"0039","intervinientes":[{"oficio":"0039","proveedor":"1181"}]},{"referencia_externa":"PVI-PRUEBA-0002","unidad_postventa":"0626.99NO EXISTE","descripcion":"PRUEBA API - NEGATIVO UPV","oficio":"0039","intervinientes":[{"oficio":"0039","proveedor":"1181"}]},{"referencia_externa":"PVI-PRUEBA-0003","unidad_postventa":"0626.03PORTAL 1.1.A","descripcion":"PRUEBA API - NEGATIVO INTERVINIENTE","oficio":"0039","intervinientes":[{"oficio":"0006"}]}]}`. El 1 es el válido (oficio `0039` con proveedor `1181`: `obrofc` único de ese oficio en la obra, leído por `sql/read` el 2026-09-24). El 2 cambia la UPV, a una inventada, la referencia y la descripción; el resto es el del 1 (si repitiera `PVI-PRUEBA-0001` saldría antes `referencia_duplicada_en_lote`: el orden es prefijo → duplicada → UPV → … → oficio → intervinientes). El 3 deja el `oficio` del parte en `0039` (si no, saldría antes `oficio_no_esta_en_la_obra`) y pone un interviniente `0006`, oficio que existe en `auxofc` (ide 6, `fecbaj` 0) y **no** tiene `obrofc` en la obra 0626: leído por `sql/read` el 2026-09-25 con `SELECT TOP (5) a.cod, a.ide, a.fecbaj, (SELECT COUNT(*) FROM dbo.obrofc o JOIN dbo.con c ON c.ide = o.obride WHERE c.emp = ? AND c.tip = ? AND c.cod = ?) AS obrofc_de_la_obra FROM dbo.auxofc a WHERE a.fecbaj = 0 AND NOT EXISTS (SELECT 1 FROM dbo.obrofc o JOIN dbo.con c ON c.ide = o.obride WHERE o.ofcide = a.ide AND c.emp = ? AND c.tip = ? AND c.cod = ?) ORDER BY a.cod` con `[1, 42, "0626", 1, 42, "0626"]` → `0006`/6, `0010`/10, `0012`/12, `0013`/13, `0014`/14, todos con `fecbaj` 0 y `obrofc_de_la_obra` 25 (la obra resuelve y tiene oficios). Antes y después, por `sql/read`: `SELECT MAX(ide) FROM dbo.con` y `SELECT MAX(cod) FROM dbo.con WHERE emp = 1 AND tip = 708 AND cod LIKE ?` con el parámetro literal `["RS26.09/[0-9][0-9][0-9][0-9]"]` (septiembre de 2026, `RS<aa>.<mm>/` del día de la prueba en hora de Madrid: **si la prueba cae en otro mes, cambiarlo**, p. ej. `RS26.10/[0-9][0-9][0-9][0-9]` en octubre). La obra está en estado 25 CER y la UPV no tiene propietario ni persona: `cliide`/`recide` saldrán 0 (la copia desde la UPV la cubren los tests). **Si el dry-run revela que Sigrid no admite partes en esa obra, se para y se consulta al humano.**  |  Verificación: `committed:false`; el parte 1 `previsto` con las cinco filas completas revisadas por el humano; el 2 `rechazado` con `unidad_postventa_no_encontrada` y el 3 `rechazado` con `interviniente_no_esta_en_la_obra`; ambos `MAX` sin cambiar; respuesta pegada en `impl_F-006.md`
- [ ] T17: **MANUAL (humano, autorización expresa)** — **`SIGRID_DOMAIN_WRITE_ENABLED` NO se toca**: ya está en `true` en la instancia (`azure-apps/sigrid_api.md` §4.1) y de ella depende el `commit` de albaranes y de `sigrid/concepto-grafico`; cerrarla los apagaría. **1) Abrir solo la llave de reclamaciones** (PowerShell, fichero ASCII sin BOM, patrón de T15): `'[{"name":"SIGRID_RECLAMACION_WRITE_ENABLED","value":"true","slotSetting":false}]' | Set-Content -Encoding ascii -NoNewline f006_abrir_reclamacion.json` y `az functionapp config appsettings set -g rg-sigrid-dev-data-api -n func-sigridapi-dev-huyke --settings "@f006_abrir_reclamacion.json"`; comprobar con el `appsettings list` de T15 (`SIGRID_RECLAMACION_WRITE_ENABLED` `true`, `SIGRID_DOMAIN_WRITE_ENABLED` `true`); si el cambio no aplica, esperar o `az functionapp stop`+`start` (`sigrid_api.md` §11). **2) Escribir un parte**: `POST $BASE/api/sigrid/partes-reclamacion` con `{"database":"ruesma","obra":"0626","usu":"prueba","commit":true,"partes":[{"referencia_externa":"PVI-PRUEBA-0001","unidad_postventa":"0626.03PORTAL 1.1.A","descripcion":"PRUEBA API - ANULAR","oficio":"0039","intervinientes":[{"oficio":"0039","proveedor":"1181"}]}]}` (el parte válido de T16, solo); después, por `sql/read`: `SELECT ide, emp, tip, cod, res, fec, est, tiemod FROM dbo.con WHERE emp = 1 AND tip = 708 AND cod = ?`, `SELECT * FROM dbo.rcp WHERE ide = ?`, `SELECT * FROM dbo.rcpint WHERE rcpide = ?`, `SELECT ide, conide, cod, valt FROM dbo.conext WHERE conide = ?`, `SELECT ide, ope, tab, tip, cod, est FROM dbo.log WHERE fec = ? AND emp = 1 AND tab = 'con' AND tip = 708 AND cod = ?` (el `fec` del parte: sin él la consulta recorre 8,5 M filas y agota el tiempo [medido]). **3) Cerrar la llave DESPUÉS de T18, no antes** (el reenvío idempotente con `commit:true` también exige la llave, R10): `'[{"name":"SIGRID_RECLAMACION_WRITE_ENABLED","value":"false","slotSetting":false}]' | Set-Content -Encoding ascii -NoNewline f006_cerrar_reclamacion.json` y `az functionapp config appsettings set -g rg-sigrid-dev-data-api -n func-sigridapi-dev-huyke --settings "@f006_cerrar_reclamacion.json"`.  |  Verificación: una fila en cada tabla (N en `rcpint`) con los valores del preview; **Posventa abre el parte en la pestaña Reclamaciones de la UPV** y ve tipo 0002, oficio, intervinientes y estado SAT como uno creado a mano; tras el cierre (paso 3), el `appsettings list` de T15 muestra `SIGRID_RECLAMACION_WRITE_ENABLED` en `false` y `SIGRID_DOMAIN_WRITE_ENABLED` **sigue en `true`**
- [ ] T18: **MANUAL (humano)** — repetir exactamente la llamada de T17 (obra `0626`, UPV `0626.03PORTAL 1.1.A`) con `"commit": true`.  |  Verificación: `idempotente`, mismo `ide` y `cod`, y `SELECT COUNT(*) FROM dbo.conext WHERE cod = 'RCPCLI' AND valt = ?` con `PVI-PRUEBA-0001` sigue en 1; después Posventa pasa el parte de prueba a NO PROCEDE desde la UI **desmarcando el envío de correo** (nunca `DELETE`); y cerrar la llave con el paso 3 de T17
- [x] T19: Ejecutar `bash harness/init.sh` en verde.  |  Verificación: termina en ENTORNO LISTO
