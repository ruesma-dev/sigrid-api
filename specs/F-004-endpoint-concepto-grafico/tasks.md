<!-- specs/F-004-endpoint-concepto-grafico/tasks.md -->
# F-004 · Tareas

Todas las lecturas contra el ERP van por `sql/read` de la API desplegada (`SIGRID_API_BASE_URL` + `SIGRID_API_FUNCTION_KEY`, patrón de `scripts/invoke_api_examples.py`). Cualquier `commit:true` exige autorización expresa del humano para esa llamada concreta. Ningún test toca red ni BBDD.

- [x] T1: **HECHA el 2026-09-05 (solo lectura)** — pareja real de parte firmado medida en `progress/explore_F-004_mediciones.md` §2: 29 columnas × 2 bases (§2.1), enlace `rcg` de 7 columnas y `gra.emp = con.emp` (§2.2), constantes de las columnas no variables (§2.3). El implementer **copia las constantes de §2.3**, no las vuelve a medir.  |  Verificación: informe existente; las constantes del constructor coinciden con §2.3
- [x] T2: **HECHA el 2026-09-05 (solo lectura)** — Q4, Q5 y Q7 resueltas en `progress/explore_F-004_mediciones.md` §1 con el defecto del diseño: formato de `cod` conservado, `guid` sin escribir, sin fila en `dbo.log`.  |  Verificación: informe existente; `design.md` §Decisiones medidas las cita
- [x] T3: Escribir `tests/test_f004_settings.py` (R4, R5) en ROJO: `Settings` con el entorno mínimo arranca con los siete defectos; `parse_int_list` acepta JSON y CSV; `allowed_write_databases` no cambia.  |  Verificación: `python -m pytest tests/test_f004_settings.py -q` falla por atributo inexistente (traza en el informe)
- [x] T4: Añadir los siete campos y `parse_int_list` a `config/settings.py`, y las claves a `local.settings.sample.json`.  |  Verificación: `python -m pytest tests/test_f004_settings.py -q` en verde
- [x] T5: Escribir `tests/test_f004_models.py` (R1, R2) en ROJO: campos obligatorios, longitudes 48/255/24, `sha256` con patrón, `commit` por defecto `false`, campos `content_type`/`clave_idempotencia` ignorados o rechazados según `extra`, y `ConceptoGraficoError.codigo`.  |  Verificación: `python -m pytest tests/test_f004_models.py -q` falla por módulo inexistente
- [x] T6: Crear `domain/models/concepto_grafico_models.py`.  |  Verificación: `python -m pytest tests/test_f004_models.py -q` en verde
- [x] T7: Escribir `tests/test_f004_document_write_guard.py` (R8; listas de R7) en ROJO: base64 no estricto, 0 bytes, tope por longitud del base64 y por bytes, firma `%PDF-` y firma ajena, `sha256` que casa y que no, listas vacías rechazan todo.  |  Verificación: `python -m pytest tests/test_f004_document_write_guard.py -q` falla
- [x] T8: Crear `infrastructure/security/document_write_guard.py` (módulo puro, sin `Settings`).  |  Verificación: `python -m pytest tests/test_f004_document_write_guard.py -q` en verde
- [x] T9: Escribir `tests/test_f004_statements.py` (R11, R13, R16, R18, R19, R20) en ROJO: SQL exacto de L1-L6 y E1-E7 para un caso conocido con `ahora` fijo; **lista completa de columnas** de E4/E5 (las 29, con las constantes de `explore_F-004_mediciones.md` §2.3) y de E6 (las 7); mismo `cod`, `emp`, `nom=nomori`, `usu` y `fec` en E4 y E5 (para `cod` y `emp`, **el mismo objeto**, `is`); L5/L6/E7 cruzan por `(emp, cod)`, nunca por `ide`; en E4 `gratipide=0`, `res=''` e `ima` bytes; en E5 `gratipide`/`res` de la petición e `ima` `None`; `vin=3`; `cla=0`, `feclee=0`, `fecalt=0`; orden E4→E5→E6; ningún `COUNT` previo por `cod`; control negativo de verbos; `DatabaseReferenceGuard` devuelve `[]` o `[documental]` y `validate(allowed=["ruesma"])` rechaza las que nombran la documental; `IdentifierGuard` rechaza una documental con caracteres raros.  |  Verificación: `python -m pytest tests/test_f004_statements.py -q` falla por módulo inexistente
- [x] T10: Crear `application/use_cases/concepto_grafico_statements.py` con las constantes de `progress/explore_F-004_mediciones.md` §2.3.  |  Verificación: `python -m pytest tests/test_f004_statements.py -q` en verde
- [x] T11: Escribir `tests/test_f004_use_case.py` (R6-R7, R9, R10, R12, R14, R15, R17, R21) en ROJO con repositorio y cursor falsos: dry-run no llama a `run_in_write_transaction` y devuelve el preview; documental vacía → `escritura_documental_deshabilitada`; base fuera de lista; concepto inexistente / `tip` distinto; `gratipide` de baja; `usu` inexistente; commit sin llaves; `work` ejecuta E1-E7 en orden y usa el `ide` de E2 en E6; relectura ≠ 1 lanza `filas_afectadas_inesperadas`; excepción en E5 no llega a E6 y sube; L5 con binario igual → `idempotente:true` sin escribir; L6 con huérfanas → aviso y `idempotente:false`; la traza no contiene el base64.  |  Verificación: `python -m pytest tests/test_f004_use_case.py -q` falla
- [x] T12: Crear `application/use_cases/attach_concepto_grafico_use_case.py`.  |  Verificación: `python -m pytest tests/test_f004_use_case.py -q` en verde
- [x] T13: Registrar la ruta `sigrid/concepto-grafico` en `function_app.py` con los `except` de `ConceptoGraficoError` (`details.codigo`) y `pyodbc.IntegrityError` (`colision_de_clave`), sin editar las rutas existentes; test de la ruta con `func.HttpRequest` falso para R3 (400 con `codigo`, 500 en lo inesperado).  |  Verificación: `python -m pytest -k f004 -q` en verde y `git diff function_app.py` solo añade
- [x] T14: Suite completa y puerta de cobertura (R5, R22): los tests de F-003 siguen en verde.  |  Verificación: `python -m pytest -q` en verde y `bash harness/init.sh` con cobertura en `[OK]`
- [x] T15: Campaña de mutación completa (rigor `critico`, sin tope), cero supervivientes o justificación escrita aceptada por el humano.  |  Verificación: `python -m harness.mutacion --feature F-004` -> `progress/mutacion_F-004.md`
- [x] T16: Actualizar `docs/ARCHITECTURE.md` (regla 1 y «PROHIBIDO escribir en `ruesma_rep`»), `CHECKPOINTS.md` C3 («`ruesma_rep` solo se lee») y `CLAUDE.md` (regla dura de `ruesma_rep`): la documental se escribe **solo** por `sigrid/concepto-grafico`.  |  Verificación: `grep -n "ruesma_rep" CLAUDE.md docs/ARCHITECTURE.md CHECKPOINTS.md` revisado uno a uno por el reviewer
- [x] T17: Documentación del ecosistema (R24): `azure-apps/sigrid_api.md` §2.1 (deja de ser cierto que nunca se escribe), §2.2 (aclarar que el `ide` que acepta `documents/read` es el **documental**, que se obtiene cruzando `ruesma.gra` y `ruesma_rep.gra` por `(emp, cod)`, nunca por `ide`; ver `progress/explore_F-004_relacion_gra.md`), §4 (siete App Settings), §5 (capa del endpoint), §7.1/§7.6 (fila nueva), §8 (tabla y §8.8 con petición, respuesta, códigos de error e idempotencia como éxito), §10 (`postventa-incidencias`); `dedicacion.md` L54, `partes.md` L548-549 y `remesas.md` L484: `ruesma_rep` no es réplica, es la base documental, `READ_WRITE`, escribible solo por ese endpoint. Commit en `azure-apps` aparte.  |  Verificación: el reviewer coteja lo escrito con el código y con `explore_ruesma_rep.md`
- [x] T18: **MANUAL (humano)** — desplegar (`func azure functionapp publish func-sigridapi-dev-huyke`) y fijar por fichero JSON las App Settings nuevas con valores de arranque **cerrados** (`SIGRID_DOCUMENT_WRITE_ENABLED=false`, `SIGRID_DOCUMENT_WRITE_DATABASE=ruesma_rep`, `SIGRID_DOCUMENT_ALLOWED_CONTIP=[708]`, `SIGRID_DOCUMENT_ALLOWED_GRATIPIDE=[35]`), comprobando que `ALLOWED_WRITE_DATABASES` sigue en `["ruesma"]`.  |  Verificación: `az functionapp config appsettings list ... --query "[?starts_with(name,'SIGRID_DOCUMENT') || name=='ALLOWED_WRITE_DATABASES']"`
- [x] T19: **MANUAL (humano)** — dry-run contra producción (nivel 2, 100 % lectura). Paso previo: identificar la huérfana real de clase 35 (`sql/read`, `database=ruesma`): `SELECT n.ide, n.cod, n.res, n.fec, r.con FROM dbo.gra n JOIN dbo.rcg r ON r.gra = n.ide LEFT JOIN ruesma_rep.dbo.gra d ON d.emp = n.emp AND d.cod = n.cod WHERE n.gratipide = 35 AND d.ide IS NULL`, y anotar su `con`. Después: `POST /api/sigrid/concepto-grafico` con un PDF de prueba, `contip=708`, `gratipide=35`, `res="PRUEBA API - BORRAR"`, sin `commit`, contra una reclamación normal **y** contra la de la huérfana; y los negativos: `conide` de una factura (`tipo_de_concepto_no_coincide`), un PNG (`tipo_de_fichero_no_permitido`), `usu` inventado (`usuario_no_valido`).  |  Verificación: respuestas pegadas en `progress/impl_F-004.md`; `committed:false`, `filas_afectadas:0`, filas E4/E5 completas y revisadas por el humano; en la huérfana `idempotente:false` con el aviso; y `sql/read` posterior confirma que `MAX(ide)` de las dos `gra` no cambió
- [ ] T20: **MANUAL (humano, autorización expresa)** — primer `commit:true` sobre una reclamación de prueba elegida por Posventa, con `SIGRID_DOCUMENT_WRITE_ENABLED=true` puesto para esa ventana; después, `documents/read` con `database=ruesma_rep`, `table=gra`, `id_column=cod`, `id_value=<cod devuelto>`, `blob_column=ima` y comparar `sha256` con el enviado; y las cuatro consultas de la propuesta §14 nivel 3 (metadatos, binario, enlace, huérfanos).  |  Verificación: `sha256` idéntico; consultas 1-3 devuelven una fila cada una y la 4 ninguna; el gráfico se abre desde la ficha en Sigrid
- [ ] T21: **MANUAL (humano)** — repetir exactamente la misma llamada con `commit:true` (nivel 4).  |  Verificación: `idempotente:true`, `filas_afectadas:0`, y `SELECT COUNT(*) FROM dbo.rcg r JOIN dbo.gra g ON g.ide = r.gra WHERE g.cod = ?` sigue devolviendo 1; limpieza desde la UI de Sigrid por Posventa
- [x] T22: Ejecutar `bash harness/init.sh` en verde.  |  Verificación: termina en ENTORNO LISTO
